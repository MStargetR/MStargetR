codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.5%
    patch:
      default:
        target: 80%
        threshold: 0.5%

comment:
  layout: 'reach,diff,flags,files'
  behavior: default
  require_changes: false

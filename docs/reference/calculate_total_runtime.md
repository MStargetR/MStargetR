# Calculate Total Runtime

This function calculates the total runtime from the start.

## Usage

``` r
calculate_total_runtime(master_list, section_name)
```

## Arguments

- master_list:

  A list containing project details and script log information.

- section_name:

  A string representing the name of the current section.

## Value

The updated `master_list` with the total runtime calculated from start.

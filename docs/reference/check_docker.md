# Function to check Docker installation, daemon, and containers

Function to check Docker installation, daemon, and containers

## Usage

``` r
check_docker()
```

## Value

Returns current status of docker

## Examples

``` r
if (FALSE) { # \dontrun{
check_docker()
} # }
```

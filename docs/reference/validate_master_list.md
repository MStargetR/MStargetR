# Validate Master List Structure

This function checks if the master list has the required structure and
data.

## Usage

``` r
validate_master_list(master_list)
```

## Arguments

- master_list:

  The master list object to validate.

## Value

NULL if the structure is valid, otherwise throws an error.

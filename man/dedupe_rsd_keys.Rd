% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qcCheckR_filter.R
\name{dedupe_rsd_keys}
\alias{dedupe_rsd_keys}
\title{Enforce unique (dataSource, dataBatch) keys on the RSD table}
\usage{
dedupe_rsd_keys(rsd)
}
\arguments{
\item{rsd}{The \code{master_list$filters$rsd} tibble (already renamed to
\code{dataSource} / \code{dataBatch}).}
}
\value{
\code{rsd} with one row per (dataSource, dataBatch) pair, in the original
row order.
}
\description{
\code{format_rsd_table()}, \code{generate_plate_summary()} and the Shiny QC tab all key
on \code{paste0(dataSource, ".", dataBatch)}. Duplicated keys turn into duplicated
column names in the exported XLSX sheet, which dplyr rejects outright. When a
key repeats, keep the most informative row (the one with the most non-NA
\%RSD values) and warn, since a duplicate signals an upstream labelling bug.
}
\keyword{internal}

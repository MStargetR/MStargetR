% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qcCheckR_export.R
\name{export_master_list_qs}
\alias{export_master_list_qs}
\title{Export Master List as QS2 File}
\usage{
export_master_list_qs(
  master_list,
  qs_nthreads = max(1L, parallel::detectCores() - 1L),
  qs_compress_level = 3L
)
}
\arguments{
\item{master_list}{A list containing project details and data.}

\item{qs_nthreads}{Integer. Worker threads passed to
\code{\link[qs2:qs_save]{qs2::qs_save()}}. Defaults to \code{max(1L, parallel::detectCores() - 1L)}.
Multi-threaded zstd is what makes large-cohort saves complete in
reasonable time; on a 54-plate cohort the previous single-threaded
gzip pass via \code{base::save()} stalled for hours.}

\item{qs_compress_level}{Integer. zstd compression level forwarded
to \code{\link[qs2:qs_save]{qs2::qs_save()}}. Default \code{3L} (qs2 default; fast with good
ratio). Higher values (up to 22) shrink the file further at the
cost of CPU time; negative values trade ratio for more speed.}
}
\value{
The updated \code{master_list} with the qs2 file exported.
}
\description{
Exports the \code{master_list} to a \code{.qs2} file (qs2 package's
multi-threaded zstd serialization format) under
\verb{<project_dir>/all/data/qs2/}, suitable for re-loading or sharing.
Load with \code{\link[qs2:qs_read]{qs2::qs_read()}}.
}
\details{
Exported (rather than internal) so that the Shiny GUI can fire this
as a detached background job after \code{\link[=qcCheckR]{qcCheckR()}} returns
(\code{qcCheckR(..., write_rda = FALSE)} followed by a separate
\code{callr::r_bg()} running this function), letting users view results
immediately while the save continues in the background.
}
\keyword{internal}

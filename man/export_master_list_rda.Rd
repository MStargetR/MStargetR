% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qcCheckR_export.R
\name{export_master_list_rda}
\alias{export_master_list_rda}
\title{Export Master List as RDA File}
\usage{
export_master_list_rda(master_list, rda_compress = FALSE)
}
\arguments{
\item{master_list}{A list containing project details and data.}

\item{rda_compress}{Passed straight through to \code{\link[=save]{save()}}'s \code{compress}
argument. Default is \code{FALSE} (no compression). On large cohorts
(~50+ plates) R's single-threaded gzip pass over the serialized
master_list can take hours and was producing an apparent hang in
the R workflow on 54-plate cohorts; an uncompressed save completes
in seconds-to-minutes at the cost of a 5-10x larger file on disk.
Set to \code{"gzip"}, \code{"bzip2"}, or \code{"xz"} to opt into compression for
archival runs.}
}
\value{
The updated \code{master_list} with the RDA file exported.
}
\description{
Exports the \code{master_list} to an RDA file under
\verb{<project_dir>/all/data/rda/}, suitable for re-loading or sharing.
}
\details{
Exported (rather than internal) so that the Shiny GUI can fire this
as a detached background job after \code{\link[=qcCheckR]{qcCheckR()}} returns
(\code{qcCheckR(..., write_rda = FALSE)} followed by a separate
\code{callr::r_bg()} running this function), letting users view results
immediately while the slow save continues in the background.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qcCheckR_dataprep.R
\name{finalise_sorted_data}
\alias{finalise_sorted_data}
\title{Finalise Sorted Data}
\usage{
finalise_sorted_data(master_list)
}
\arguments{
\item{master_list}{A list containing project details and sorted peak area data.}
}
\value{
The updated master list with finalised sorted data, including the
additive \code{sample_class} column described above.
}
\description{
This function finalises the sorted data by adding sample type factors, reversing the order of sample types, and setting the sample data source.
}
\details{
In addition to the legacy two-value \code{sample_type} column (\code{"qc"} / \code{"sample"}),
a new \code{sample_class} column is populated with three mutually-exclusive
values:
\itemize{
\item \code{"qc"} — the chosen pooled QC (project-level \code{qc_type}).
\item \code{"sample"} — biological study samples.
\item \code{"other"} — blanks, SIL/IS injections, conditioning samples,
and non-chosen QCs (any \code{sample_tags} value that is not the chosen
\code{qc_type}). These must never be treated as biological samples for
QC-vs-sample statistics (medians, drift models, RSD, etc.).
}

Downstream code should prefer \code{sample_class} for QC-vs-sample splits.
\code{sample_type} is kept populated the old way for backward compatibility
with existing exports and downstream consumers.
}
\keyword{internal}

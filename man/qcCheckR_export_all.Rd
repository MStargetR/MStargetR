% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qcCheckR_export.R
\name{qcCheckR_export_all}
\alias{qcCheckR_export_all}
\title{Export All Project Outputs}
\usage{
qcCheckR_export_all(
  master_list,
  write_rda = TRUE,
  qs_nthreads = max(1L, parallel::detectCores() - 1L),
  qs_compress_level = 3L
)
}
\arguments{
\item{master_list}{A list containing project details and data.}

\item{write_rda}{Logical. When \code{TRUE} (default) the master_list qs2 file is
written synchronously as part of the export step. Set to \code{FALSE} when the
caller intends to write it out-of-band (e.g. the Shiny GUI fires a
detached background save so results can render before the qs2 write
completes). When \code{FALSE}, callers are responsible for invoking
\code{\link[=export_master_list_qs]{export_master_list_qs()}} themselves. The name is retained from the
prior \code{.rda} API to avoid churning every caller; the underlying
output is now \code{.qs2}.}

\item{qs_nthreads}{Forwarded to \code{\link[=export_master_list_qs]{export_master_list_qs()}}; see its
documentation for the default.}

\item{qs_compress_level}{Forwarded to \code{\link[=export_master_list_qs]{export_master_list_qs()}}; see
its documentation for the default.}
}
\value{
The updated \code{master_list} with exported files.
}
\description{
Exports the \code{master_list} to XLSX, HTML, and qs2 formats, including summary tables, QC metrics, and processed data.
}
\examples{
\dontrun{
master_list <- qcCheckR_export_all(master_list)
}
}
\keyword{internal}

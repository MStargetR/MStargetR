% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resultsExplorer.R
\name{resultsExplorerR}
\alias{resultsExplorerR}
\title{Generate Results Explorer plots and summary from R}
\usage{
resultsExplorerR(
  data,
  project_dir = NULL,
  advanced_plots = FALSE,
  warn_thr = 20,
  fail_thr = 30,
  qc_label = "qc",
  class_map = NULL
)
}
\arguments{
\item{data}{One of:
\itemize{
\item A data frame of samples (rows) x metabolites (columns) plus
metadata columns (e.g. \code{sample_type}, \code{sample_run_index}).
\item A \code{qcCheckR()} master_list (auto-extracts
\code{data$concentration$corrected} and \code{filters$rsd}).
\item A \code{batchCorrectR()} result (auto-extracts \code{corrected_data},
\code{qc_rsd_before}, \code{qc_rsd_after}).
}}

\item{project_dir}{Character or NULL. When \code{advanced_plots = TRUE},
plots are written to \code{file.path(project_dir, "all", "figures", "results_explorer")}. Required for disk writes; otherwise ignored.}

\item{advanced_plots}{Logical. When \code{TRUE} and \code{project_dir} is
non-null, every plot is saved as PDF + HTML. Plots are returned
in-memory either way. Default \code{FALSE}.}

\item{warn_thr, fail_thr}{Numeric thresholds for the RSD-based
pass/warning/fail classification. Defaults \code{20} and \code{30} (mirrors
the Shiny app defaults).}

\item{qc_label}{Character. Sample-type value identifying QC
injections, used to compute RSDs from a bare data frame. Default
\code{"qc"} (case-insensitive).}

\item{class_map}{Optional named character vector mapping metabolite
name -> class. If \code{NULL}, classes are derived from metabolite
names via the same regex the GUI uses.}
}
\value{
A list with two elements:
\describe{
\item{\code{plots}}{Named list of plots; each entry is
\verb{list(static = <ggplot>, interactive = <plotly>)}.}
\item{\code{summary}}{Tibble with one row per metabolite: \code{metabolite},
\code{class}, \code{rsd}, \code{status}.}
}
}
\description{
Recreates every plot the Shiny app's Results Explorer tab renders --
RSD distribution, pass/fail donut, quality-by-class, RSD scatter,
concentration-vs-RSD scatter, per-metabolite box plot, run-order
scatter (before/after when batch correction is supplied), and
concentration heatmap -- so they can be inspected from an R script
and (optionally) written to disk.
}
\details{
Accepts a plain data frame, a \code{qcCheckR()} master_list, or a
\code{batchCorrectR()} result; dispatch picks the relevant tables. When
\code{advanced_plots = TRUE} and \code{project_dir} is supplied, every plot is
written to \verb{<project_dir>/all/figures/results_explorer/} as both a
static \code{.pdf} (via \code{ggplot2::ggsave}) and an interactive \code{.html}
(via \code{htmlwidgets::saveWidget}).
}
\examples{
\dontrun{
  # From a batchCorrectR result, save figures to disk:
  bc <- batchCorrectR(my_data, project_dir = my_project)
  re <- resultsExplorerR(bc, project_dir = my_project,
                          advanced_plots = TRUE)

  # From a plain data frame, return plots in memory only:
  re <- resultsExplorerR(my_data, qc_label = "PQC")
  re$plots$rsd_histogram$static  # ggplot
  re$plots$rsd_histogram$interactive  # plotly
}
}

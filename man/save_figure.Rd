% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures_export.R
\name{save_figure}
\alias{save_figure}
\title{Save one figure to disk as both PDF and HTML}
\usage{
save_figure(plot, name, project_dir, module, width = 10, height = 7, dpi = 300)
}
\arguments{
\item{plot}{A ggplot, plotly, or \code{list(static, interactive)}.}

\item{name}{Character. File basename (no extension).}

\item{project_dir, module}{Forwarded to \code{\link[=figures_dir]{figures_dir()}}.}

\item{width, height}{Numeric. PDF dimensions (inches). Defaults 10 x 7.}

\item{dpi}{Numeric. PDF resolution. Default 300.}
}
\value{
Character. The path stem (\verb{<dir>/<name>}) invisibly.
}
\description{
Accepts a plot in one of three shapes:
\itemize{
\item a \verb{list(static = <ggplot>, interactive = <plotly>)} (preferred — both
formats use their best source);
\item a bare \code{ggplot} object (PDF from ggsave; HTML from \code{ggplotly()} wrap);
\item a bare \code{plotly} object (HTML only; PDF skipped with a single message).
}
}
\details{
Returns the paths written (without extension) invisibly.
}
\keyword{internal}

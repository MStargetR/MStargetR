# PeakForgeR_mzml.R

mzML file import, parsing, and chromatogram handling functions. Includes
mzR MRM finding, peak boundary detection, and lipid matching.

## Usage

``` r
ANPC_EXCLUDE_PATTERN
```

## Format

An object of class `character` of length 1.

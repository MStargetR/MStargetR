# Export XLSX File

This function exports the `master_list` data to an XLSX file, including
user guide, QC metrics, and processed data.

## Usage

``` r
export_xlsx_file(master_list)
```

## Arguments

- master_list:

  A list containing project details and data.

## Value

The updated `master_list` with the XLSX file exported.

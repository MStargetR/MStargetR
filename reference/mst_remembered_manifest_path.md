# Path of the per-project remembered plate-grouping manifest

Path of the per-project remembered plate-grouping manifest

## Usage

``` r
mst_remembered_manifest_path(input_directory)
```

## Arguments

- input_directory:

  Project directory containing `raw_data/`.

## Value

Absolute-ish path to `plate_grouping.csv` at the project root.
